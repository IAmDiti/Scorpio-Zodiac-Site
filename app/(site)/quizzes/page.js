import { PagePlaceholder } from '@/components/page-placeholder'

export const metadata = { title: 'Scorpio Quizzes' }

export default function QuizzesPage() {
  return (
    <PagePlaceholder
      eyebrow="Play · Discover · Share"
      title="Scorpio quizzes"
      blurb="Take any quiz free. A free account unlocks your result and keeps your history."
      phase="Phase 5 (quiz engine)"
    />
  )
}
