import { PagePlaceholder } from '@/components/page-placeholder'

export const metadata = { title: 'Scorpio Compatibility' }

export default function CompatibilityPage() {
  return (
    <PagePlaceholder
      eyebrow="Compatibility"
      title="Scorpio & every sign"
      blurb="Pick a partner's sign for a breakdown of passion, trust, communication and friendship."
      phase="Phase 3 (horoscope + compatibility)"
    />
  )
}
